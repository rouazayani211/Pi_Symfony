{% extends 'index.html.twig' %}

{% block title %}Edit Evenement{% endblock %}

{% block body %}
<div class="container" style="margin-top: 150px;">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card">
                <div class="card-header">
                    <h2 class="text">Edit Evenement :</h2>
                </div>
                <div class="card-body"  class="btn btn-primary btn-sm" >
                  {{ include('evenement/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
                <div class="card-footer">
                    <a href="{{ path('app_evenement_index') }}" class="btn btn-secondary">Back to list</a>

                  </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

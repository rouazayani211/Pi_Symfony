{% extends 'base.html.twig' %}

{% block title %}Video Highlights{% endblock %}

{% block body %}
    <h1>Game Highlights</h1>
    <iframe
        width="100%"
        height="500px"
        src="{{ youtubeEmbedUrl }}"
        frameborder="0"
        allowfullscreen>
    </iframe>

    <a href="{{ path('app_equipe_show', {'idEquipe': equipe.idEquipe}) }}" class="btn btn-secondary">Back to Equipe</a>
{% endblock %}

{# templates/videofront.html.twig #}

<!-- This template reuses the front-end base template -->
<!DOCTYPE html>
<html lang="en">
<head>
  <title>Team Video Highlights</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Include necessary stylesheets -->
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="{{ asset('fonts/icomoon/style.css') }}">
  <link rel="stylesheet" href="{{ asset('css/bootstrap/bootstrap.css') }}">
  <link rel="stylesheet" href="{{ asset('css/style.css') }}">
</head>
<body>
  <div class="site-wrap">
    <!-- Use the same header structure -->
    <header class="site-navbar py-4" role="banner">
      <div class="container">
        <div class="d-flex align-items-center">
    
          <div class="ml-auto">
            <nav class="site-navigation position-relative text-right" role="navigation">
              <ul class="site-menu main-menu js-clone-nav mr-auto d-none d-lg-block">
                <li><a href="{{ path('app_equipe_front') }}" class="nav-link">Equipe</a></li>
              </ul>
            </nav>
          </div>
        </div>
      </div>
    </header>

    <!-- Video content section -->
    <div class="container mt-5">
      <h1>Game Highlights</h1>
      <iframe
          width="100%"
          height="500px"
          src="{{ youtubeEmbedUrl }}"
          frameborder="0"
          allowfullscreen>
      </iframe>

      <!-- Back button to the main Equipe page -->
      <a href="{{ path('app_equipe_front') }}" class="btn btn-primary">Back to Equipes</a>
    </div>

    <!-- Footer section -->
    <footer class="footer-section">
      <div class="container">
        <!-- Add footer content here -->
      </div>
    </footer>
  </div>

  <!-- Include necessary scripts -->
  <script src="{{ asset('js/jquery-3.3.1.min.js') }}"></script>
  <script src="{{ asset('js/bootstrap.min.js') }}"></script>
</body>
</html>

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails de la ticket</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #faf3e0; /* Couleur de fond beige */
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.1); /* Ombre portée */
            padding: 30px;
            text-align: center;
            width: 80%;
            max-width: 600px;
            animation: fadeIn 0.5s ease; /* Animation d'apparition */
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        h1 {
            color: #333;
            margin-bottom: 20px;
        }

        .event-details {
            font-size: 16px;
            margin-bottom: 20px;
        }

        .event-name {
            color: #ff5733;
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .event-price {
            color: #4CAF50;
            font-size: 18px;
            font-weight: bold;
            margin-top: 20px;
        }

        /* Style pour le logo */
        .logo {
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Détails de la ticket</h1>
        <div class="event-details">
            <p class="event-name">Num siege : {{ ticket.getNumSiege() }}</p>
            <p class="event-details">Statut Ticket  : {{ ticket.getStatutTicket() }}</p>
            <p class="event-details">Date : {{ ticket.getDateEvenement()|date('Y-m-d') }}</p>
            <p class="event-price">Prix en TND : {{ ticket.getPrix() }}</p>
            <p class="event-price">Prix en euros : {{ prixEnEuros }}</p>
            <p class="event-price">Prix en dollars : {{ prixEnDollars }}</p>
            <p class="event-price">Prix en yuan : {{ prixEnYuan }}</p>
        </div>
    </div>
</body>
</html>
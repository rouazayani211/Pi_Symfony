<!DOCTYPE html>
<html lang="en">
<head>
  <title>Equipe Details</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="{{ asset('fonts/icomoon/style.css') }}">
  <link rel="stylesheet" href="{{ asset('css/bootstrap/bootstrap.css') }}">
  <link rel="stylesheet" href="{{ asset('css/style.css') }}">
</head>
<body>
  <div class="site-wrap">
    <!-- Include header with logo and navigation -->
    <header class="site-navbar py-4" role="banner">
      <div class="container">
        <div class="d-flex align-items-center">
          <div class="site-logo">
            <a href="{{ path('app_equipe_front') }}">
              <img src="{{ asset('images/logo20.png') }}" alt="Logo" style="width:200px; height:auto;">
            </a>
          </div>
          <div class="ml-auto">
            <nav class="site-navigation position-relative text-right" role="navigation">
              <ul class="site-menu main-menu js-clone-nav mr-auto d-none d-lg-block">
                <li><a href="{{ path('app_equipe_front') }}" class="nav-link">Equipe</a></li>
              </ul>
            </nav>
          </div>
        </div>
      </div>
    </header>

    <!-- Hero section with background image -->
    <div class="hero overlay" style="background-image: url('{{ asset('images/bg_3.jpg') }}');">
      <div class="container">
        <div class="row align-items-center">
          <div class="col-lg-6 mx-auto text-center">
            <h1 class="text-white">Equipe Details</h1>
             <!-- Main content section -->
    <div class="container mt-5">
      <h2>{{ equipe.nom }}</h2>
      <p><strong>Region:</strong> {{ equipe.region }}</p>
      <p><strong>Ligue:</strong> {{ equipe.ligue }}</p>
      <p><strong>Classement:</strong> {{ equipe.classement }}</p>
       <a href="{{ path('app_equipe_video_front', {'equipeNom': equipe.nom}) }}" class="btn btn-primary">Show Highlights</a>

    </div>
          </div>
        </div>
      </div>
    </div>

   

    <!-- Footer section -->
    <footer class="footer-section">
      <div class="container">
        <!-- Footer content -->
      </div>
    </footer>
  </div>

  <!-- Include necessary scripts -->
  <script src="{{ asset('js/jquery-3.3.1.min.js') }}"></script>
  <script src="{{ asset('js/bootstrap.min.js') }}"></script>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <title>Joueur Details</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="{{ asset('fonts/icomoon/style.css') }}">
  <link rel="stylesheet" href="{{ asset('css/bootstrap/bootstrap.css') }}">
  <link rel="stylesheet" href="{{ asset('css/style.css') }}">
</head>
<body>
  <div class="site-wrap">
    <!-- Include header with logo and navigation -->
    <header class="site-navbar py-4" role="banner">
      <div class="container">
        <div class="d-flex align-items-center">
          <div class="site-logo">
            <a href="{{ path('app_equipe_front') }}">
              <img src="{{ asset('images/logo20.png') }}" alt="Logo" style="width:200px; height:auto;">
            </a>
          </div>
          <div class="ml-auto">
            <nav class="site-navigation position-relative text-right" role="navigation">
              <ul class="site-menu main-menu js-clone-nav mr-auto d-none d-lg-block">
                <li class="active"><a href="{{ path('app_joueur_front') }}" class="nav-link">Joueurs</a></li>
              </ul>
            </nav>
          </div>
        </div>
      </div>
    </header>

    <!-- Hero section with background image -->
    <div class="hero overlay" style="background-image: url('{{ asset('images/bg_3.jpg') }}');">
      <div class="container">
        <div class="row align-items-center">
          <div class="col-lg-6 mx-auto text-center">
            <h1 class="text-white">Joueur Details</h1>
             <!-- Main content section -->
    <div class="container mt-5">
      <h2>{{ joueur.nom }}</h2>
      <p><strong>Prenom:</strong> {{ joueur.prenom }}</p>
      <p><strong>Age:</strong> {{ joueur.age }}</p>
      <p><strong>Numéro:</strong> {{ joueur.numero }}</p>
      <p><strong>Position:</strong> {{ joueur.position }}</p>
      <p><strong>IDEquipe:</strong> {{ joueur.id_Equipe }}</p>
       <a class="btn btn-primary" href="{{ path('app_joueur_summary_front', {'nom': joueur.nom}) }}">Voir le résumé</a>

    </div>
          </div>
        </div>
      </div>
    </div>

   

    <!-- Footer section -->
    <footer class="footer-section">
      <div class="container">
        <!-- Footer content -->
      </div>
    </footer>
  </div>

  <!-- Include necessary scripts -->
  <script src="{{ asset('js/jquery-3.3.1.min.js') }}"></script>
  <script src="{{ asset('js/bootstrap.min.js') }}"></script>
</body>
</html>
